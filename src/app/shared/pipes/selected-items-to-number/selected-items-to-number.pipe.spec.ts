import { SelectedItemsToNumberPipe } from './selected-items-to-number.pipe';

describe('SelectedItemsToNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedItemsToNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
